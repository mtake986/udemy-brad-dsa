const countOccurrences = (str, char) => str.split(char).length - 1;
  // let occurence = 0;
  // for (let i = 0; i < str.length; i++) {
  //   if (str[i] === char) occurence += 1;
  // }
  // return occurence;


module.exports = countOccurrences;
